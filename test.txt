
const sliderValue = ref(100); // Initial value of the slider
const min = ref(0); // Minimum value of the slider
const max = ref(100); // Maximum value of the slider
// const min2 = ref(50); // Minimum value of the slider
// const max2 = ref(100); // Maximum value of the slider
// const priceGap = ref(10);

const minPrice = ref(840);
const maxPrice = ref(22529);
// const leftProgress = computed(() => (minPrice.value / max.value) * 100 + "%");
// const rightProgress = computed(
//   () => 100 - (minPrice.value / max.value) * 100 + "%"
// );
let presentage = ref(0);

const rangeS = document.querySelectorAll("input[type=range]");

const priceGap = 1000; // Set your price gap value
const rang1 = computed(() => 1000);
const rang2 = computed(() => 500);
// const range = document.querySelector(".slider .progress");
const leftProgress = ref((rang1.value / maxPrice.value) * 100 + "%");
const rightProgress = ref((rang2.value / maxPrice.value) * 100 + "%");
const CheckGape = (e, id) => {
  // if (maxVal - minVal < priceGap) {
  //   console.log("inside true statment");
  //   if (e.target.className === "range-min") {
  //     rang1.value = maxVal - priceGap;
  //     console.log("renge-min");
  //   } else {
  //     rang2.value = minVal + priceGap;
  //     console.log("renge-max");
  //   }
  // } else {
  //   console.log("inside false statment");
  //   leftProgress.value = (minVal / e.max) * 100 + "%";
  //   console.log(leftProgress.value);
  //   // range.style.left = (minVal / e.max) * 100 + "%";
  //   rightProgress.value = 100 - (maxVal / e.max) * 100 + "%";
  //   console.log(rightProgress.value);
  //   // range.style.right = 100 - (maxVal / e.max) * 100 + "%";
  // }
};

const getRang = (e) => {
  const rangeS = document.querySelectorAll("input[type=range]");
  rangeS.forEach(function (el) {
    el.oninput = function () {
      var slide1 = parseFloat(rangeS[0].value),
        slide2 = parseFloat(rangeS[1].value);

      // if (slide1 > slide2 >= priceGap) {
      //   [slide1, slide2] = [slide2, slide1];
      // }
      if (slide1 - slide2 < priceGap) {
        console.log(slide1, slide2, "gap is less 1000");
      }

      // if (slide2 - slide1 >= priceGap && slide2 <= rangeS[1].max) {
      //   if (e.target.className === "range-min") {
      //     rangeS[0].value = slide1;
      //     leftProgress.value = (slide1 / rangeS[0].max) * 100 + "%";
      //     console.log("range-min");
      //   } else {
      //     rangeS[1].value = slide2;

      //     rightProgress.value = 100 - (slide2 / rangeS[1].max) * 100 + "%";

      //     console.log("range-max");
      //   }
      // }

      // console.log(leftProgress.value, slide2);
      // numberS[0].value = slide1;
      // numberS[1].value = slide2;
    };
  });
};
